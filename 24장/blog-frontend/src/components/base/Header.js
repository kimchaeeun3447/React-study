import React from 'react';
import styled from 'styled-components';
/**
 * 회원가입 / 로그인 페이지의 레이아웃을 담당하는 컴포넌트입니다.
 */

/* 화면 전체를 채움 - AuthTemplate 을 TM_FILENAME_BASE 로 교체*/
const HeaderBlock = styled.div``;

const Header = () => {
  return (
    <HeaderBlock>
      
    </HeaderBlock>
  );
};

export default Header;